#brexComp.jl contains functions for computing stationary equilibrium and transition paths.

#Function SE_compute computes stationary equilibrium for given parameters par. SE is the initial guess for stationary equilibrium.
function SE_compute(par,SE)
    println("function SE_compute is just a placeholder...")
    return SE
end
